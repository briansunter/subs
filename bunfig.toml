[test]
# Integration tests can run concurrently using Fastify inject()
# Unit tests run sequentially by default
concurrentTestGlob = ["**/integration/**/*.test.ts"]
maxConcurrency = 10
# Preload test setup file to configure environment before any modules load
preload = "./test/setup.ts"
